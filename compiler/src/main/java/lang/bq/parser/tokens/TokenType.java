package lang.bq.parser.tokens;

public enum TokenType {
    PUNCTUATION,
    OPERATOR,
    IDENTIFIER,
    KEYWORD,
    NUMBER,
    STRING,
    BOOLEAN,
    TYPE,

    FUNCTION,
    NODE,
    METHOD,
    IF,
    FOR,
    WHILE,

    CALL,
    EXPRESSION,
    SCRIPT,
    ASSIGN
}
